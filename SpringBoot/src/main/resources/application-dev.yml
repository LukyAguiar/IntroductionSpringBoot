#DEV - Environment

#----------------------------- Config Properties

#app.message=This is the property file fo the ${spring.application.name} for DEV

#spring.datasource.driver-class-name=org.h2.Driver
#spring.datasource.url=jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
#spring.datasource.username=sa
#spring.datasource.password=sa

#----------------------------- Config YML

app:
  message: This is the property file fo the ${spring.application.name} for DEV and YML

spring:
  datasource:
      driver-class-name: org.h2.Driver
      url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
      username: sa
      password: sa